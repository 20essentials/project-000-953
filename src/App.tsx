import { Background } from '@/components/Background.tsx';
import { TopSlider } from '@/components/TopSlider.tsx';
import { BottomSlider } from '@/components/BottomSlider.tsx';
import { useLeftandRightKey } from '@/hooks/useLeftandRightKey';

export const App = () => {
  ((((((((((((((((((((useLeftandRightKey()))))))))))))))))))));

  return (
    <>
      <Background />
      <TopSlider />
      <BottomSlider />
    </>
  );
};
